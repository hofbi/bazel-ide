load("@aspect_gazelle_prebuilt//:def.bzl", "aspect_gazelle")

aspect_gazelle(
    name = "gazelle",
    command = "fix",
    languages = ["cc"],
)

aspect_gazelle(
    name = "gazelle.check",
    command = "fix",
    languages = ["cc"],
    mode = "diff",
)
